#!/bin/sh

set -e

basedir=$(dirname $0)

(
	cd $basedir && \
		autoreconf -ivf && \
		$basedir/configure $@ && \
		make -C $basedir fetch
)

echo "###"
echo "### You can now run make or configure for customization"
echo "###"

exit 0
